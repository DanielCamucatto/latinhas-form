export type FormValues = {
    title: string; 
    description: string; 
    deadline: Date;
}

export type Demand = {
    id: string;
    title: string; 
    description: string; 
    deadline: Date;
}